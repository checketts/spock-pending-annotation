Pending annotation
==================

Simple annotation that runs a feature and flags it as ignored if it fails (not implemented yet, instead flags it as passing for now).
However, if the feature passed then it is flagged as a failure, indicating that the annotation should be removed